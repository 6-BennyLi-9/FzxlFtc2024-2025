package org.firstinspires.ftc.teamcode.message;

public class DriveMessage extends DriveBufMessage{
	public DriveMessage(double bufX, double bufY, double bufTurn) {
		super(bufX, bufY, bufTurn);
	}
}
